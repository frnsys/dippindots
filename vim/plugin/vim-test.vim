" run currently focused unittest (requires vim-test and nose)
nnoremap <C-S-t> :TestNearest<cr>
" also :TestFile to run all tests in file and :TestSuite to run all
" :TestVisit jumps to the last-visited test file, useful when debugging
" :TestLast runs the last-visited test, useful when debugging
