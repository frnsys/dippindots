[global]
    # refer to <https://github.com/knopwob/dunst/blob/master/dunstrc>
    # put colors in quotation marks so they aren't mistaken as comments

    # font = Source Code Pro Light 9
    font = Input Light 9
    markup = yes
    plain_text = no
    word_wrap = yes
    alignment = left
    indicate_hidden = yes
    show_age_threshold = 60
    idle_threshold = 120
    ignore_newline = no
    transparency = 0
    shrink = no
    sticky_history = no
    history_length = 20
    show_indicators = no
    monitor = 0

    # mouse/keyboard/none
    follow = mouse

    # sort by urgency
    sort = yes

    # %s=summary, %b=body
    format = "%s\n\n%b"

    # [{width}]x{height}[+/-{x}+/-{y}]
    # height is in number of notifications
    # everything else in pixels
    geometry = "400x5-8+26"

    notification_height = 0
    line_height = 2
    separator_height = 5
    padding = 12
    horizontal_padding = 12
    icon_position = left
    max_icon_size = 128
    frame_width = 1
    frame_color = "#1cbc5f"

    # auto/foreground/frame/a color
    separator_color = "#303030"

    dmenu = "/home/ftseng/.bin/dmenu -p dunst:"
    browser = /usr/local/bin/firefox -new-tab

[shortcuts]
# close notification
    close = ctrl+shift+period

    # close all notifications
    close_all = ctrl+shift+space

    # redisplay last message (nb: grave is "`")
    history = ctrl+grave

    # context menu
    context = ctrl+space


[urgency_low]
    background = "#202020"
    foreground = "#ffffff"
    timeout = 2
    icon = ""

[urgency_normal]
    background = "#202020"
    foreground = "#ffffff"
    timeout = 2
    icon = ""

[urgency_critical]
    background = "#ef3838"
    foreground = "#ffffff"
    frame_color = "#bf1c1c"
    timeout = 0
    icon = ""
